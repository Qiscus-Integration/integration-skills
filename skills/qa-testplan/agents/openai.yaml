# agents/openai.yaml — metadata for Codex UI (optional, ignored by Claude Code)
# Remove this file if you don't need Codex support

interface:
  display_name: "QA Test Plan Generator"
  short_description: "Generate manual test cases, Gherkin scenarios, and automation specs from PRD"
  default_prompt: "Use $qa-testplan to generate a complete test plan including manual test cases, Gherkin BDD scenarios, automation test structure, API tests, and regression coverage from the PRD output."

policy:
  # true  = Codex can invoke this skill automatically based on context
  # false = user must invoke explicitly with $skill-name
  allow_implicit_invocation: false
