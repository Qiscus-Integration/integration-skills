# agents/openai.yaml — metadata for Codex UI (optional, ignored by Claude Code)
# Remove this file if you don't need Codex support

interface:
  display_name: "AI Ecosystem Self-Assessment"
  short_description: "Assess AI agent maturity, tooling, and workflow adoption"
  default_prompt: "Use $self-assessment to assess my AI agent ecosystem maturity and generate the assessment report in Bahasa Indonesia."

policy:
  # true  = Codex can invoke this skill automatically based on context
  # false = user must invoke explicitly with $skill-name
  allow_implicit_invocation: true
