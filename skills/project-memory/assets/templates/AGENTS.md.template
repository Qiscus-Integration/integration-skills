# Project: [PROJECT NAME]

> This file is automatically read by OpenAI Codex CLI at the start of every session.
> Keep it concise — total size of all AGENTS.md files must stay under 32 KiB.
> Details live in `.ai-memory/memory/` (or `.codex/memory/` if Claude Code is not used).

## Stack
- **Backend**: [e.g. Laravel 11, PHP 8.3]
- **Frontend**: [e.g. React 18 + Vite / Blade]
- **Database**: [e.g. PostgreSQL 15]
- **Cache/Queue**: [e.g. Redis + Laravel Horizon]
- **Auth**: [e.g. Laravel Sanctum / JWT]
- **Deploy**: [e.g. Docker + Nginx / Vercel]

## Project Type
[Monolith / API + SPA / Microservice]

Main entry points:
- Backend: `[path/to/controllers/or/router]`
- Frontend: `[path/to/pages/or/views]`

## Required Rules (READ THIS FIRST)
- [e.g. NEVER modify existing migrations — always create a new one]
- [e.g. All business logic goes in the Service layer, not in Controllers]
- [e.g. Use the Repository pattern for all DB queries]
- [e.g. Every new endpoint must have a Feature Test]

## Architecture Overview
```
[Brief architecture layer diagram, e.g.:]
Request → Controller → Service → Repository → Model
                    ↘ Event → Listener
```

## Memory Files (read when you need details)
- **Module map**: `.ai-memory/memory/modules.md`
- **Architecture & decisions**: `.ai-memory/memory/architecture.md`
- **Conventions**: `.ai-memory/memory/conventions.md`
- **Active tasks**: `.ai-memory/tasks/active.md`

## Working Agreements
- [e.g. Always run `npm test` after modifying JavaScript files]
- [e.g. Ask for confirmation before adding new production dependencies]
- [e.g. Run `php artisan test` before committing]

## Recent Changes
<!-- Update this after every task! Format: [YYYY-MM-DD] Short description by @developer -->
- [YYYY-MM-DD] Initial project memory setup

## Instructions for AI Agent

After completing every task:
1. Update the "Recent Changes" section in this file
2. If there are architecture changes → update `.ai-memory/memory/architecture.md`
3. If there are new modules → update `.ai-memory/memory/modules.md`
4. If a task is unfinished → record progress in `.ai-memory/tasks/active.md`
5. Commit all memory changes together with the code